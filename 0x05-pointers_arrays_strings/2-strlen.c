#include "main.h"
#incldue <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: variable
 * @i: variable
 * Return: Always 0
 */
int _strlen(char *s)
{
	int i;

	for (i =0; s[i] != '\0'; i++)
	{
	continue;
	}
	return (i);
}
