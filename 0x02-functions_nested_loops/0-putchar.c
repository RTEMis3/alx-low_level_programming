#include <stdio.h>
#include <unistd.h>


/**
 * main - Entry point
 * _putchar - writes the character c to stdout
 * @c: The character to print
 * Return: Always zero (success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
	putchar("\"_putchar")
	}
	putchar('\n');
	return (0);
}
